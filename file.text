<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Trang Chủ</title>
    <style>
      /* Loader đen toàn màn hình */
      #loader {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: black;
        z-index: 9999;
      }

      body {
        margin: 0;
        padding: 0;
        background-color: black; /* Nền đen khi chưa load ảnh */
        background-image: url("background.png");
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        height: 100vh;
        font-family: sans-serif;
        user-select: none;
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
      }

      img {
        user-drag: none;
        -webkit-user-drag: none;
        /* pointer-events: none; bỏ đi hoặc để cho tất cả ảnh khác nếu muốn */
      }

      .menu-container {
        position: absolute;
        top: 20px;
        left: 20px;
        z-index: 1000;
      }

      .menu-icon,
      .menu-image {
        font-size: 30px;
        color: white;
        cursor: pointer;
        user-select: none;
        display: block;
      }

      .menu-image {
        display: none;
        width: 40px;
        height: 40px;
        border-radius: 50%;
        object-fit: cover;
        pointer-events: auto; /* Thêm dòng này */
      }

      .menu-content {
        display: none;
        position: absolute;
        top: 60px;
        left: 0;
        background-color: rgba(0, 0, 0, 0.7);
        color: white;
        padding: 15px;
        border-radius: 8px;
        min-width: 150px;
      }

      .menu-content ul {
        list-style: none;
        margin: 0;
        padding: 0;
      }

      .menu-content li {
        margin-bottom: 10px;
        cursor: pointer;
        font-size: 18px;
        padding: 10px 15px;
        text-align: center;
        transition: background-color 0.3s, transform 0.2s;
        border-radius: 6px;
      }

      .menu-content li:hover {
        background-color: rgba(255, 255, 255, 0.2);
        transform: scale(1.05);
      }
    </style>
  </head>
  <body
    oncontextmenu="return false"
    onselectstart="return false"
    ondragstart="return false"
  >
    <!-- Loader đen xuất hiện khi tải -->
    <div id="loader"></div>

    <div class="menu-container">
      <div class="menu-icon" id="hamburger" onclick="toggleMenu()">&#9776;</div>
      <img
        src="menu-bar.jpg"
        alt="Icon"
        class="menu-image"
        id="icon"
        onclick="toggleMenu()"
      />
      <div class="menu-content" id="menu">
        <ul>
          <li onclick="window.location.href='index.html'">Trang chủ</li>
          <li>Tiếng Anh</li>
          <li>Tiếng Nhật</li>
        </ul>
      </div>
    </div>

    <script>
      function toggleMenu() {
        const icon = document.getElementById("icon");
        const hamburger = document.getElementById("hamburger");
        const menu = document.getElementById("menu");

        const hamburgerVisible =
          window.getComputedStyle(hamburger).display !== "none";

        if (hamburgerVisible) {
          // Ấn dấu 3 gạch: ẩn hamburger, hiện icon + menu
          hamburger.style.display = "none";
          icon.style.display = "block";
          menu.style.display = "block";
        } else {
          // Ấn icon: ẩn icon + menu, hiện lại hamburger
          hamburger.style.display = "block";
          icon.style.display = "none";
          menu.style.display = "none";
        }
      }

      // Ngăn Ctrl+U, Ctrl+S, Ctrl+C, Ctrl+Shift+I, F12
      document.addEventListener("keydown", function (e) {
        if (
          (e.ctrlKey && ["u", "s", "c"].includes(e.key.toLowerCase())) ||
          (e.ctrlKey && e.shiftKey && e.key.toLowerCase() === "i") ||
          e.key === "F12"
        ) {
          e.preventDefault();
        }
      });

      // Loader biến mất khi trang tải xong
      window.addEventListener("load", function () {
        const loader = document.getElementById("loader");
        loader.style.display = "none";
      });
    </script>
  </body>
</html>
